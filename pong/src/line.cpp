#include <SFML/Graphics.hpp>

class Player{
	private:
		int x; //location in x
		int y; //location in y
    	sf::RectangleShape player(sf::Vector2f(10.f,50.f);
	public:
		Player(){
			
		}
		void setPos(int y){
			x = x;
			y = y;
		}
		void drawBall(){
    		shape.setFillColor(sf::Color::Green);
		}
};
